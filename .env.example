# Configuración de variables de entorno para CallTech
# Copia este archivo como .env y configura los valores apropiados

# Clave secreta para Flask (genera una nueva para producción)
SECRET_KEY=tu-clave-secreta-muy-segura-aqui

# URL de la base de datos
# Para desarrollo local con SQLite:
# DATABASE_URL=sqlite:///calltech.db
# Para Heroku Postgres (se configura automáticamente):
# DATABASE_URL=postgresql://usuario:password@host:puerto/database

# Modo debug (solo para desarrollo)
DEBUG=False

# Puerto (Heroku lo configura automáticamente)
PORT=5000
